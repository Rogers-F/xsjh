/**
 * Branding constants
 *
 * Default site name shown before the backend-delivered public settings
 * (GET /api/public-settings) arrive. Keep every display fallback on this
 * constant so a future rebrand is a one-line change.
 */
export const DEFAULT_SITE_NAME = '星算'
